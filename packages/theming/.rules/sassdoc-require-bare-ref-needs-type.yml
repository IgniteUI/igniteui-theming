id: sassdoc-require-bare-ref-needs-type
language: sassdoc
severity: warning
message: 'Bare @requires reference needs a type — use {function}, {mixin}, {variable}, or {placeholder}'
rule:
  kind: tag_require
  all:
    - has:
        kind: see_reference
        regex: '^[^$%]'
    - not:
        has:
          kind: see_type
