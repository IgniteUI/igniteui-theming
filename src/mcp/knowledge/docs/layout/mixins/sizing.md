# sizing() mixin

Defines @property rules for size levels:
- --ig-size-small
- --ig-size-medium
- --ig-size-large

## Sass example
```scss
@include sizing();
```

## Notes
- Typically included once at the root scope.
